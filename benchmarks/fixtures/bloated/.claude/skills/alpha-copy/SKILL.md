---
name: alpha
description: Use when the user asks to summarise a changelog into release notes, but differently.
---
# Alpha (variant)
This one groups by author instead of by type, which is a completely different behaviour
for the same skill name, so which one wins depends on load order.
